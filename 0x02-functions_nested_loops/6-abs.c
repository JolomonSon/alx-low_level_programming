#include "main.h"

/**
 * _abs - Computes absolute value of integers.
 * @absolute: Integer operand/ argument.
 *
 * Return: absolute.
 */
int _abs(int n)
{
	if (n < 0)
	{
		n = (-1) * n;
	}
	return (n);
}
